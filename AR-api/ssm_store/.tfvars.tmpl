# Generated by envsubst in CI; set Bitbucket repo variables for each.
# Local: use staging.tfvars (gitignored) or pass -var-file=staging.tfvars

ENV                                   = "$ENV"
owner                                 = "$OWNER"
cost_center                           = "$COST_CENTER"
ecr_repository_name                   = "$ECR_REPOSITORY_NAME"
image_tag                             = "$IMAGE_TAG"
db_name                               = "$DB_NAME"
db_username                           = "$DB_USERNAME"
db_password                           = "$DB_PASSWORD"
rds_instance_class                    = "$RDS_INSTANCE_CLASS"
redis_node_type                       = "$REDIS_NODE_TYPE"
worker_runtime                        = "$WORKER_RUNTIME"
alarm_email                           = "$ALARM_EMAIL"

PROJECT_NAME                           = "$PROJECT_NAME"
FRONTEND_URL                           = "$FRONTEND_URL"
CORS_ORIGINS                           = "$CORS_ORIGINS"
CELERY_BROKER_URL                      = "$CELERY_BROKER_URL"
CELERY_RESULT_BACKEND                  = "$CELERY_RESULT_BACKEND"
DATABASE_URL                           = "$DATABASE_URL"
STRIPE_WEBHOOK_SECRET                  = "$STRIPE_WEBHOOK_SECRET"
STRIPE_SECRET_KEY                      = "$STRIPE_SECRET_KEY"
STRIPE_PRICE_ID_MONTHLY                = "$STRIPE_PRICE_ID_MONTHLY"
STRIPE_PRICE_ID_YEARLY                 = "$STRIPE_PRICE_ID_YEARLY"
STRIPE_API_VERSION                     = "$STRIPE_API_VERSION"
CLERK_SECRET_KEY                       = "$CLERK_SECRET_KEY"
CLERK_JWKS_URL                         = "$CLERK_JWKS_URL"
CLERK_SESSION_TOKEN_EXPIRES_IN_SECONDS = "$CLERK_SESSION_TOKEN_EXPIRES_IN_SECONDS"
SMTP_HOST                              = "$SMTP_HOST"
SMTP_PORT                              = "$SMTP_PORT"
SMTP_USERNAME                          = "$SMTP_USERNAME"
SMTP_PASSWORD                          = "$SMTP_PASSWORD"
SMTP_REGION                            = "$SMTP_REGION"
SMTP_USE_TLS                           = "$SMTP_USE_TLS"
EMAIL_SENDER                           = "$EMAIL_SENDER"
EMAIL_SENDER_NAME                      = "$EMAIL_SENDER_NAME"
SMS_BASE_URL                           = "$SMS_BASE_URL"
SMS_X_API_KEY                          = "$SMS_X_API_KEY"
SMS_API_KEY                            = "$SMS_API_KEY"
STARTUP_ENABLE_DDB_CHECK               = "$STARTUP_ENABLE_DDB_CHECK"
DDB_TABLE_NAME                         = "$DDB_TABLE_NAME"
DDB_REGION                             = "$DDB_REGION"
CANTO_AUTH_URL                         = "$CANTO_AUTH_URL"
CANTO_APP_ID                           = "$CANTO_APP_ID"
CANTO_APP_SECRET                       = "$CANTO_APP_SECRET"
SENTRY_DSN                             = "$SENTRY_DSN"
SLACK_WEBHOOK_URL                      = "$SLACK_WEBHOOK_URL"

